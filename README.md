# Simple calculator in Python with PyQt6

To run the calculator execute the following commands:

``` shell
python -m venv venv
source venv/bin/activate
python -m pip install pyqt6

python calculator.py
```

